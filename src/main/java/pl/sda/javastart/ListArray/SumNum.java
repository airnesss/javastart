package pl.sda.javastart.ListArray;

import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

//public class SumNum {
    //Napisz program, który pozwoli zsumować dowolną ilość liczb od użytkownika. Aplikacja powinna przerwać wczytywanie
    // liczb, gdy użytkownik wprowadzi polecenie "exit".
    //
    //Po wczytaniu liczb wyświetl je na ekranie oraz podaj wynik ich sumowania w postaci a + b + c + ... + N = wynik.
    // Do wczytywania danych wykorzystaj metodę rekurencyjną.
//    public static final String EXIT = "EXIT";
//   private static Scanner sc = new Scanner(System.in);

  //  public static void main(String[] args) {
     //   List<Double>numbers = new ArrayList<>();
  //      fillList(numbers);



    //}
    //metoda rekurencyjna do wczytywania liczb, przekazujemy jej jako param obikt reprezentujacy liste która chcemy wypełnic liczbami
  //  static void fillList (List<Double>list){
     //   System.out.println("Podaj kolejną liczbę lub wpisz EXIT");
     //   String input = sc.nextLine();
      //  if (input.equals(SumNum.EXIT)){
         //   return;
        //    try {
           //     Double num = Double.valueOf(input);
//            }catch (NumberFormatException e){
            //    System.err.println("Liczba w niepoprawnym formacie");
          //  }
         //   fillList(list);
      //  }
        //metoda do wyswietlania danych
      //  static void printData (List<Double>list){
         //   StringBuilder sb = new StringBuilder();
          //  double sum = 0;
         //   for (Double aDouble : list) {
             //   sb.append(aDouble);
             //   sb.append('+');
            //    sum = sum +aDouble;
                
         //   }
         //   sb.replace(sb.length()-1,sb.length(),"=");
          //  sb.append(sum);
        //    System.out.println(sb.toString());

     //   }

  //  }
//}

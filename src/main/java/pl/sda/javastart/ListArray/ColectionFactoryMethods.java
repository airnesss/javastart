package pl.sda.javastart.ListArray;

import java.util.List;

class ColectionFactoryMethods {
    //collections factory methods, czyli specjalne metody, przeznaczone przede wszystkim do tworzenia niewielkich list,
    // gdy znamy elementy, które mają się w niej znaleźć.
    public static void main(String[] args) {
       // List<Integer>numbers = List.of(5,10,15);
       // for (Integer number : numbers) {
         //   System.out.println(number);
            
        }
    }
//}
